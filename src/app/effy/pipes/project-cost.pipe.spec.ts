import { ProjectCostPipe } from './project-cost.pipe';

describe('ProjectCostPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectCostPipe();
    expect(pipe).toBeTruthy();
  });
});
